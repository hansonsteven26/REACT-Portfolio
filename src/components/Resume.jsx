import { Box, Typography } from '@mui/material';

const Resume = () => {
  return (
    <Box id="resume">
      <Typography variant="h2">Resume</Typography>
    </Box>
  );
};

export default Resume;
